{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "{{projectDescription}}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node ./server/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverhub-mvc": "0.0.93"
  }
}
